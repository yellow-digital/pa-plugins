export default () => {
   /**
   * Run some tests
   */
  // Eager extraction
  //  const resp = await extract(eventSchema, 'remember meeting today at 4pm with John')
  //  console.log(resp)

  // Test call could be return from LLM
  // app.run(`FUNCTION('todo', 'meeting today at 1pm')`)
  // app.write('remember I have a meeting today at 1pm')
  // app.write(new UserMessage('remember I have a meeting today at 1pm with Paul'))
  // app.invoke()
  // await app.isIdle()
  // app.clear()
  // app.write(new UserMessage('I have a meeting tomorrow at 2pm with Paul'))
  // app.invoke()

  // app.backend.chat()
}